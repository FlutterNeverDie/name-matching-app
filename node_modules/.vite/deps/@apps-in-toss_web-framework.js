//#region node_modules/@apps-in-toss/bridge-core/dist/index.js
var NativeWindow$1 = class {
	get _window() {
		if (typeof window !== "undefined") return window;
		return {
			ReactNativeWebView: { postMessage: () => {} },
			__GRANITE_NATIVE_EMITTER: { on: () => () => {} },
			__CONSTANT_HANDLER_MAP: {}
		};
	}
	postMessage(message) {
		const webView = this._window.ReactNativeWebView;
		if (!webView) throw new Error("ReactNativeWebView is not available in browser environment");
		webView.postMessage(JSON.stringify(message));
	}
	on(event, callback) {
		const emitter = this._window.__GRANITE_NATIVE_EMITTER;
		if (!emitter) throw new Error("__GRANITE_NATIVE_EMITTER is not available");
		return emitter.on(event, callback);
	}
	getConstant(method) {
		const constantHandlerMap = this._window.__CONSTANT_HANDLER_MAP;
		if (constantHandlerMap && method in constantHandlerMap) return constantHandlerMap[method];
		throw new Error(`${method} is not a constant handler`);
	}
};
var nativeWindow$1 = new NativeWindow$1();
var createEventId$1 = () => Math.random().toString(36).substring(2, 15);
var deserializeError$1 = (value) => {
	if (value && value.__isError) {
		const err = new Error(value.message);
		for (const [key, val] of Object.entries(value)) err[key] = val;
		return err;
	}
	return value;
};
function createAsyncBridge$1(method) {
	return (...args) => {
		const eventId = createEventId$1();
		const emitters = [];
		const unsubscribe = () => {
			for (const remove of emitters) remove();
		};
		return new Promise((resolve, reject) => {
			emitters.push(nativeWindow$1.on(`${method}/resolve/${eventId}`, (data) => {
				unsubscribe();
				resolve(data);
			}));
			emitters.push(nativeWindow$1.on(`${method}/reject/${eventId}`, (error) => {
				unsubscribe();
				reject(deserializeError$1(error));
			}));
			nativeWindow$1.postMessage({
				type: "method",
				functionName: method,
				eventId,
				args
			});
		});
	};
}
function createEventBridge$1(method) {
	return (args) => {
		const eventId = createEventId$1();
		const removes = [nativeWindow$1.on(`${method}/onEvent/${eventId}`, (data) => {
			args.onEvent(data);
		}), nativeWindow$1.on(`${method}/onError/${eventId}`, (error) => {
			args.onError(deserializeError$1(error));
		})];
		nativeWindow$1.postMessage({
			type: "addEventListener",
			functionName: method,
			eventId,
			args: args.options
		});
		return () => {
			nativeWindow$1.postMessage({
				type: "removeEventListener",
				functionName: method,
				eventId
			});
			removes.forEach((remove) => remove());
		};
	};
}
function createConstantBridge$1(method) {
	return () => {
		return nativeWindow$1.getConstant(method);
	};
}
//#endregion
//#region node_modules/@apps-in-toss/web-bridge/dist/bridge.js
var createEvents = function() {
	return {
		emit: function emit(event, args) {
			for (var callbacks = this.events[event] || [], i = 0, length = callbacks.length; i < length; i++) callbacks[i](args);
		},
		events: {},
		on: function on(event, cb) {
			var _this = this;
			var _this_events, _event;
			((_this_events = this.events)[_event = event] || (_this_events[_event] = [])).push(cb);
			return function() {
				var _this_events_event;
				_this.events[event] = (_this_events_event = _this.events[event]) === null || _this_events_event === void 0 ? void 0 : _this_events_event.filter(function(i) {
					return cb !== i;
				});
			};
		}
	};
};
if (typeof window !== "undefined") window.__GRANITE_NATIVE_EMITTER = createEvents();
var closeView = createAsyncBridge$1("closeView");
var generateHapticFeedback = createAsyncBridge$1("generateHapticFeedback");
var share = createAsyncBridge$1("share");
var setSecureScreen = createAsyncBridge$1("setSecureScreen");
var setScreenAwakeMode = createAsyncBridge$1("setScreenAwakeMode");
var getNetworkStatus = createAsyncBridge$1("getNetworkStatus");
var setIosSwipeGestureEnabled = createAsyncBridge$1("setIosSwipeGestureEnabled");
var openURL = createAsyncBridge$1("openURL");
var openPermissionDialog = createAsyncBridge$1("openPermissionDialog");
var getPermission = createAsyncBridge$1("getPermission");
var requestPermission = createAsyncBridge$1("requestPermission");
createConstantBridge$1("setClipboardText");
createConstantBridge$1("getClipboardText");
createConstantBridge$1("fetchContacts");
createConstantBridge$1("fetchAlbumPhotos");
createConstantBridge$1("getCurrentLocation");
createConstantBridge$1("openCamera");
var appLogin = createAsyncBridge$1("appLogin");
var eventLog = createAsyncBridge$1("eventLog");
var getTossShareLink = createAsyncBridge$1("getTossShareLink");
var setDeviceOrientation = createAsyncBridge$1("setDeviceOrientation");
var checkoutPayment = createAsyncBridge$1("checkoutPayment");
var saveBase64Data = createAsyncBridge$1("saveBase64Data");
var appsInTossSignTossCert = createAsyncBridge$1("appsInTossSignTossCert");
var getGameCenterGameProfile = createAsyncBridge$1("getGameCenterGameProfile");
var openGameCenterLeaderboard = createAsyncBridge$1("openGameCenterLeaderboard");
var submitGameCenterLeaderBoardScore = createAsyncBridge$1("submitGameCenterLeaderBoardScore");
var getAnonymousKey = createAsyncBridge$1("getAnonymousKey");
var getUserKeyForGame = createAsyncBridge$1("getUserKeyForGame");
var grantPromotionReward = createAsyncBridge$1("grantPromotionReward");
var grantPromotionRewardForGame = createAsyncBridge$1("grantPromotionRewardForGame");
var getIsTossLoginIntegratedService = createAsyncBridge$1("getIsTossLoginIntegratedService");
createAsyncBridge$1("getServerTime");
createAsyncBridge$1("requestReview");
var getLocale = createConstantBridge$1("getLocale");
var getSchemeUri = createConstantBridge$1("getSchemeUri");
var getPlatformOS = createConstantBridge$1("getPlatformOS");
var getOperationalEnvironment = createConstantBridge$1("getOperationalEnvironment");
var getTossAppVersion = createConstantBridge$1("getTossAppVersion");
var getDeviceId = createConstantBridge$1("getDeviceId");
var getGroupId = createConstantBridge$1("getGroupId");
var contactsViral = createEventBridge$1("contactsViral");
createEventBridge$1("startUpdateLocation");
var onVisibilityChangedByTransparentServiceWeb = createEventBridge$1("onVisibilityChangedByTransparentServiceWeb");
//#endregion
//#region node_modules/@apps-in-toss/types/dist/index.js
var PermissionError = class extends Error {
	constructor({ methodName, message }) {
		super();
		this.name = `${methodName} permission error`;
		this.message = message;
	}
};
var FetchAlbumPhotosPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "fetchAlbumPhotos",
			message: "사진첩 권한이 거부되었어요."
		});
	}
};
var FetchContactsPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "fetchContacts",
			message: "연락처 권한이 거부되었어요."
		});
	}
};
var OpenCameraPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "openCamera",
			message: "카메라 권한이 거부되었어요."
		});
	}
};
var Accuracy = /* @__PURE__ */ ((Accuracy2) => {
	Accuracy2[Accuracy2["Lowest"] = 1] = "Lowest";
	Accuracy2[Accuracy2["Low"] = 2] = "Low";
	Accuracy2[Accuracy2["Balanced"] = 3] = "Balanced";
	Accuracy2[Accuracy2["High"] = 4] = "High";
	Accuracy2[Accuracy2["Highest"] = 5] = "Highest";
	Accuracy2[Accuracy2["BestForNavigation"] = 6] = "BestForNavigation";
	return Accuracy2;
})(Accuracy || {});
var GetCurrentLocationPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "getCurrentLocation",
			message: "위치 권한이 거부되었어요."
		});
	}
};
var StartUpdateLocationPermissionError = GetCurrentLocationPermissionError;
var GetClipboardTextPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "getClipboardText",
			message: "클립보드 읽기 권한이 거부되었어요."
		});
	}
};
var SetClipboardTextPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "setClipboardText",
			message: "클립보드 쓰기 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region node_modules/@apps-in-toss/web-bridge/dist/index.js
var NativeWindow = class {
	get _window() {
		if (typeof window !== "undefined") return window;
		return {
			ReactNativeWebView: { postMessage: () => {} },
			__GRANITE_NATIVE_EMITTER: { on: () => () => {} },
			__CONSTANT_HANDLER_MAP: {}
		};
	}
	postMessage(message) {
		const webView = this._window.ReactNativeWebView;
		if (!webView) throw new Error("ReactNativeWebView is not available in browser environment");
		webView.postMessage(JSON.stringify(message));
	}
	on(event, callback) {
		const emitter = this._window.__GRANITE_NATIVE_EMITTER;
		if (!emitter) throw new Error("__GRANITE_NATIVE_EMITTER is not available");
		return emitter.on(event, callback);
	}
	getConstant(method) {
		const constantHandlerMap = this._window.__CONSTANT_HANDLER_MAP;
		if (constantHandlerMap && method in constantHandlerMap) return constantHandlerMap[method];
		throw new Error(`${method} is not a constant handler`);
	}
};
var nativeWindow = new NativeWindow();
var createEventId = () => Math.random().toString(36).substring(2, 15);
var deserializeError = (value) => {
	if (value && value.__isError) {
		const err = new Error(value.message);
		for (const [key, val] of Object.entries(value)) err[key] = val;
		return err;
	}
	return value;
};
function createAsyncBridge(method) {
	return (...args) => {
		const eventId = createEventId();
		const emitters = [];
		const unsubscribe = () => {
			for (const remove of emitters) remove();
		};
		return new Promise((resolve, reject) => {
			emitters.push(nativeWindow.on(`${method}/resolve/${eventId}`, (data) => {
				unsubscribe();
				resolve(data);
			}));
			emitters.push(nativeWindow.on(`${method}/reject/${eventId}`, (error) => {
				unsubscribe();
				reject(deserializeError(error));
			}));
			nativeWindow.postMessage({
				type: "method",
				functionName: method,
				eventId,
				args
			});
		});
	};
}
function createEventBridge(method) {
	return (args) => {
		const eventId = createEventId();
		const removes = [nativeWindow.on(`${method}/onEvent/${eventId}`, (data) => {
			args.onEvent(data);
		}), nativeWindow.on(`${method}/onError/${eventId}`, (error) => {
			args.onError(deserializeError(error));
		})];
		nativeWindow.postMessage({
			type: "addEventListener",
			functionName: method,
			eventId,
			args: args.options
		});
		return () => {
			nativeWindow.postMessage({
				type: "removeEventListener",
				functionName: method,
				eventId
			});
			removes.forEach((remove) => remove());
		};
	};
}
function createConstantBridge(method) {
	return () => {
		return nativeWindow.getConstant(method);
	};
}
var Storage = {
	getItem: createAsyncBridge("getStorageItem"),
	setItem: createAsyncBridge("setStorageItem"),
	removeItem: createAsyncBridge("removeStorageItem"),
	clearItems: createAsyncBridge("clearItems")
};
var SEMVER_REGEX = /^[v^~<>=]*?(\d+)(?:\.([x*]|\d+)(?:\.([x*]|\d+)(?:\.([x*]|\d+))?(?:-([\da-z\\-]+(?:\.[\da-z\\-]+)*))?(?:\+[\da-z\\-]+(?:\.[\da-z\\-]+)*)?)?)?$/i;
var isWildcard = (val) => [
	"*",
	"x",
	"X"
].includes(val);
var tryParse = (val) => {
	const num = parseInt(val, 10);
	return isNaN(num) ? val : num;
};
var coerceTypes = (a, b) => {
	return typeof a === typeof b ? [a, b] : [String(a), String(b)];
};
var compareValues = (a, b) => {
	if (isWildcard(a) || isWildcard(b)) return 0;
	const [aVal, bVal] = coerceTypes(tryParse(a), tryParse(b));
	if (aVal > bVal) return 1;
	if (aVal < bVal) return -1;
	return 0;
};
var parseVersion = (version) => {
	if (typeof version !== "string") throw new TypeError("Invalid argument: expected a string");
	const match = version.match(SEMVER_REGEX);
	if (!match) throw new Error(`Invalid semver: '${version}'`);
	const [, major, minor, patch, build, preRelease] = match;
	return [
		major,
		minor,
		patch,
		build,
		preRelease
	];
};
var compareSegments = (a, b) => {
	const maxLength = Math.max(a.length, b.length);
	for (let i = 0; i < maxLength; i++) {
		const result = compareValues(a[i] ?? "0", b[i] ?? "0");
		if (result !== 0) return result;
	}
	return 0;
};
var compareVersions = (v1, v2) => {
	const seg1 = parseVersion(v1);
	const seg2 = parseVersion(v2);
	const preRelease1 = seg1.pop();
	const preRelease2 = seg2.pop();
	const mainCompare = compareSegments(seg1, seg2);
	if (mainCompare !== 0) return mainCompare;
	if (preRelease1 && preRelease2) return compareSegments(preRelease1.split("."), preRelease2.split("."));
	if (preRelease1) return -1;
	if (preRelease2) return 1;
	return 0;
};
function isMinVersionSupported(minVersions) {
	if (createConstantBridge("getOperationalEnvironment")() === "sandbox") return true;
	const currentVersion = createConstantBridge("getTossAppVersion")();
	const minVersion = createConstantBridge("getPlatformOS")() === "ios" ? minVersions.ios : minVersions.android;
	if (minVersion === void 0) return false;
	if (minVersion === "always") return true;
	if (minVersion === "never") return false;
	return compareVersions(currentVersion, minVersion) >= 0;
}
function processProductGrant(params) {
	return createAsyncBridge("processProductGrant")(params);
}
var IAP = {
	createOneTimePurchaseOrder: (params) => {
		const isIAPSupported = isMinVersionSupported({
			android: "5.219.0",
			ios: "5.219.0"
		});
		const noop = () => {};
		if (!isIAPSupported) return noop;
		const isProcessProductGrantSupported = isMinVersionSupported({
			android: "5.231.1",
			ios: "5.230.0"
		});
		const { options, onEvent, onError } = params;
		const sku = options.sku ?? options.productId;
		if (!isProcessProductGrantSupported) {
			const v1 = () => {
				createAsyncBridge("iapCreateOneTimePurchaseOrder")({ productId: sku }).then((response) => {
					Promise.resolve(options.processProductGrant({ orderId: response.orderId })).then(() => {
						onEvent({
							type: "success",
							data: response
						});
					}).catch((error) => {
						onError(error);
					});
				}).catch((error) => {
					onError(error);
				});
				return noop;
			};
			return v1();
		}
		return createEventBridge("requestOneTimePurchase")({
			options: { sku },
			onEvent: async (event) => {
				if (event.type === "purchased") {
					const isProductGranted = await options.processProductGrant({ orderId: event.data.orderId });
					await processProductGrant({
						orderId: event.data.orderId,
						isProductGranted
					}).catch(onError);
				} else onEvent(event);
			},
			onError: (error) => {
				onError(error);
			}
		});
	},
	createSubscriptionPurchaseOrder: (params) => {
		const isSupported = isMinVersionSupported({
			android: "5.248.0",
			ios: "5.249.0"
		});
		const noop = () => {};
		if (!isSupported) return noop;
		const { options, onEvent, onError } = params;
		const { sku, offerId, processProductGrant: handleProcessProductGrant } = options;
		return createEventBridge("requestSubscriptionPurchase")({
			options: {
				sku,
				offerId: offerId ?? null
			},
			onEvent: async (event) => {
				if (event.type === "purchased") {
					const isProductGranted = await handleProcessProductGrant({
						orderId: event.data.orderId,
						subscriptionId: event.data.subscriptionId
					});
					await processProductGrant({
						orderId: event.data.orderId,
						isProductGranted
					}).catch(onError);
				} else onEvent(event);
			},
			onError: (error) => {
				onError(error);
			}
		});
	},
	getProductItemList: createAsyncBridge("iapGetProductItemList"),
	getPendingOrders: createAsyncBridge("getPendingOrders"),
	getCompletedOrRefundedOrders: createAsyncBridge("getCompletedOrRefundedOrders"),
	completeProductGrant: createAsyncBridge("completeProductGrant"),
	getSubscriptionInfo: createAsyncBridge("getSubscriptionInfo")
};
var _getSafeAreaInsets = createConstantBridge("getSafeAreaInsets");
function getSafeAreaInsets() {
	return _getSafeAreaInsets();
}
function subscribeSafeAreaInsets({ onEvent }) {
	return createEventBridge("safeAreaInsetsChange")({
		onEvent,
		onError: () => {}
	});
}
var SafeAreaInsets = {
	get: createConstantBridge("getSafeAreaInsets"),
	subscribe: subscribeSafeAreaInsets
};
var GoogleAdMob = {
	loadAppsInTossAdMob: Object.assign(createEventBridge("loadAppsInTossAdMob"), { isSupported: createConstantBridge("loadAppsInTossAdMob_isSupported") }),
	showAppsInTossAdMob: Object.assign(createEventBridge("showAppsInTossAdMob"), { isSupported: createConstantBridge("showAppsInTossAdMob_isSupported") }),
	isAppsInTossAdMobLoaded: Object.assign(createAsyncBridge("isAppsInTossAdMobLoaded"), { isSupported: createConstantBridge("isAppsInTossAdMobLoaded_isSupported") })
};
var graniteEvent = { addEventListener: (event, { onEvent, onError, options }) => createEventBridge(event)({
	onEvent,
	onError: onError ?? (() => {}),
	options
}) };
var appsInTossEvent = { addEventListener: (event, { onEvent, onError, options }) => createEventBridge(event)({
	onEvent,
	onError: onError ?? (() => {}),
	options
}) };
var env = { getDeploymentId: createConstantBridge("getDeploymentId") };
var deploymentId = createConstantBridge("deploymentId");
var brandDisplayName = createConstantBridge("brandDisplayName");
var brandIcon = createConstantBridge("brandIcon");
var brandPrimaryColor = createConstantBridge("brandPrimaryColor");
var getAppsInTossGlobals = () => {
	return {
		deploymentId: deploymentId(),
		brandDisplayName: brandDisplayName(),
		brandIcon: brandIcon(),
		brandPrimaryColor: brandPrimaryColor()
	};
};
var tdsEvent = { addEventListener: (event, { onEvent, onError, options }) => createEventBridge(event)({
	onEvent,
	onError: onError ?? (() => {}),
	options
}) };
var partner = {
	addAccessoryButton: createAsyncBridge("addAccessoryButton"),
	removeAccessoryButton: createAsyncBridge("removeAccessoryButton")
};
var requestPermission$1 = createAsyncBridge("requestPermission");
var getPermission$1 = createAsyncBridge("getPermission");
var openPermissionDialog$1 = createAsyncBridge("openPermissionDialog");
function createPermissionFunction({ permission, handler, error }) {
	const permissionFunction = async (...args) => {
		if (await requestPermission$1(permission) === "denied") throw new error();
		return handler(...args);
	};
	permissionFunction.getPermission = () => getPermission$1(permission);
	permissionFunction.openPermissionDialog = () => openPermissionDialog$1(permission);
	return permissionFunction;
}
var fetchAlbumPhotos = createPermissionFunction({
	handler: (options) => {
		return createAsyncBridge("fetchAlbumPhotos")(options);
	},
	permission: {
		name: "photos",
		access: "read"
	},
	error: FetchAlbumPhotosPermissionError
});
var fetchContacts = createPermissionFunction({
	handler: (options) => {
		return createAsyncBridge("fetchContacts")(options);
	},
	permission: {
		name: "contacts",
		access: "read"
	},
	error: FetchContactsPermissionError
});
var getCurrentLocation = createPermissionFunction({
	handler: (options) => {
		return createAsyncBridge("getCurrentLocation")(options);
	},
	permission: {
		name: "geolocation",
		access: "access"
	},
	error: GetCurrentLocationPermissionError
});
var openCamera = createPermissionFunction({
	handler: (options) => {
		return createAsyncBridge("openCamera")(options);
	},
	permission: {
		name: "camera",
		access: "access"
	},
	error: OpenCameraPermissionError
});
var setClipboardText = createPermissionFunction({
	handler: (options) => {
		return createAsyncBridge("setClipboardText")(options);
	},
	permission: {
		name: "clipboard",
		access: "write"
	},
	error: SetClipboardTextPermissionError
});
var getClipboardText = createPermissionFunction({
	handler: () => {
		return createAsyncBridge("getClipboardText")();
	},
	permission: {
		name: "clipboard",
		access: "read"
	},
	error: GetClipboardTextPermissionError
});
var getPermission2 = createAsyncBridge("getPermission");
var openPermissionDialog2 = createAsyncBridge("openPermissionDialog");
var startUpdateLocation = (params) => {
	return createEventBridge("updateLocationEvent")({
		...params,
		onError: (error) => {
			const locationError = new StartUpdateLocationPermissionError();
			if (error instanceof Error && error.name === locationError.name) return params.onError(locationError);
			return params.onError(error);
		}
	});
};
startUpdateLocation.getPermission = () => getPermission2({
	name: "geolocation",
	access: "access"
});
startUpdateLocation.openPermissionDialog = () => openPermissionDialog2({
	name: "geolocation",
	access: "access"
});
var loadFullScreenAd = Object.assign(createEventBridge("loadFullScreenAd"), { isSupported: createConstantBridge("loadFullScreenAd_isSupported") });
var showFullScreenAd = Object.assign(createEventBridge("showFullScreenAd"), { isSupported: createConstantBridge("showFullScreenAd_isSupported") });
var attachedBannerRegistry = /* @__PURE__ */ new WeakMap();
function getOrCreateAttachedBanner(element, createAttachedBanner) {
	const attachedBanner = attachedBannerRegistry.get(element);
	if (attachedBanner) return attachedBanner;
	const createdAttachedBanner = createAttachedBanner();
	let isDestroyed = false;
	const registeredAttachedBanner = { destroy() {
		if (isDestroyed) return;
		isDestroyed = true;
		if (attachedBannerRegistry.get(element) === registeredAttachedBanner) attachedBannerRegistry.delete(element);
		createdAttachedBanner.destroy();
	} };
	attachedBannerRegistry.set(element, registeredAttachedBanner);
	return registeredAttachedBanner;
}
function resetAttachedBannerRegistry() {
	attachedBannerRegistry = /* @__PURE__ */ new WeakMap();
}
var openURL$1 = createAsyncBridge("openURL");
function openUrlOpener(url) {
	return openURL$1(getWebSchemeOrUri(url));
}
function getWebSchemeOrUri(uri) {
	return ["http://", "https://"].some((protocol) => uri.startsWith(protocol)) ? supertossWeb(uri) : uri;
}
function supertossWeb(uri) {
	return `supertoss://web?url=${encodeURIComponent(uri)}&external=true`;
}
var DEFAULT_SDK_URL = "https://static.toss.im/ads/sdk/toss-ads-space-kit-1.3.0.js";
var DEFAULT_TIMEOUT_MS = 15e3;
var pendingLoad = null;
function getAdsSdk() {
	if (typeof window === "undefined") return;
	return window.TossAdsSpaceKit;
}
function loadAdsSdk() {
	if (typeof window === "undefined" || typeof document === "undefined") return Promise.reject(/* @__PURE__ */ new Error("Ads SDK can only be loaded in a browser environment."));
	const existing = getAdsSdk();
	if (existing) return Promise.resolve(existing);
	if (pendingLoad) return pendingLoad;
	const promise = new Promise((resolve, reject) => {
		const script = document.createElement("script");
		const cleanup = () => {
			script.removeEventListener("load", handleLoad);
			script.removeEventListener("error", handleError);
			window.clearTimeout(timeoutId);
			pendingLoad = null;
		};
		const handleLoad = () => {
			const sdk = getAdsSdk();
			if (sdk) {
				cleanup();
				resolve(sdk);
				return;
			}
			cleanup();
			reject(/* @__PURE__ */ new Error("Ads SDK script loaded but window.TossAdsSpaceKit was not exposed."));
		};
		const handleError = () => {
			cleanup();
			reject(/* @__PURE__ */ new Error(`Failed to load Ads SDK script from ${DEFAULT_SDK_URL}.`));
		};
		const timeoutId = window.setTimeout(() => {
			cleanup();
			reject(/* @__PURE__ */ new Error(`Loading Ads SDK timed out after ${DEFAULT_TIMEOUT_MS}ms.`));
		}, DEFAULT_TIMEOUT_MS);
		script.addEventListener("load", handleLoad);
		script.addEventListener("error", handleError);
		script.async = true;
		script.src = DEFAULT_SDK_URL;
		document.head.appendChild(script);
	});
	pendingLoad = promise;
	return promise;
}
var fetchTossAd = Object.assign(createEventBridge("fetchTossAd"), { isSupported: createConstantBridge("fetchTossAd_isSupported") });
var tossAdEventLog = createAsyncBridge("tossAdEventLog");
var SUPPORTED_STYLE_IDS = /* @__PURE__ */ new Set(["1", "2"]);
var INVALID_AD_GROUP_ID_ERROR_MESSAGE = "잘못된 요청이에요. 필요한 값이 비어 있어요.";
function normalizeAdGroupId(adGroupId) {
	return adGroupId.trim();
}
function createInvalidAdGroupIdError() {
	return new Error(INVALID_AD_GROUP_ID_ERROR_MESSAGE);
}
function createInvalidAdGroupIdResponse() {
	return {
		resultType: "FAIL",
		error: { reason: INVALID_AD_GROUP_ID_ERROR_MESSAGE }
	};
}
function fetchTossAdPromise(options) {
	return new Promise((resolve, reject) => {
		if (!fetchTossAd.isSupported()) {
			reject(/* @__PURE__ */ new Error("fetchTossAd is not supported in this environment."));
			return;
		}
		const adGroupId = normalizeAdGroupId(options.adGroupId);
		if (adGroupId.length === 0) {
			reject(createInvalidAdGroupIdError());
			return;
		}
		return fetchTossAd({
			options: {
				...options,
				adGroupId
			},
			onEvent: resolve,
			onError: reject
		});
	});
}
function normalizeAdResponse(adResponse) {
	const ads = Array.isArray(adResponse.ads) ? adResponse.ads.filter((ad) => SUPPORTED_STYLE_IDS.has(String(ad.styleId))) : [];
	return {
		requestId: adResponse.requestId ?? "",
		status: adResponse.status ?? "OK",
		ads,
		ext: adResponse.ext
	};
}
function normalizeApiResponse(raw) {
	if (isApiResponse(raw)) {
		if (raw.resultType !== "SUCCESS") return raw;
		if (!raw.success) return {
			resultType: "FAIL",
			error: { reason: "fetchTossAd returned SUCCESS without payload" }
		};
		return {
			...raw,
			success: normalizeAdResponse(raw.success)
		};
	}
	if (isAdResponse(raw)) return {
		resultType: "SUCCESS",
		success: normalizeAdResponse(raw)
	};
	return {
		resultType: "FAIL",
		error: { reason: "Invalid response from fetchTossAd" }
	};
}
function isApiResponse(payload) {
	return Boolean(payload && typeof payload === "object" && "resultType" in payload);
}
function isAdResponse(payload) {
	return Boolean(payload && typeof payload === "object" && "ads" in payload);
}
function createCustomAdFetcher() {
	return async (_endpoint, request) => {
		const spaceUnitId = normalizeAdGroupId(request.spaceUnitId);
		if (spaceUnitId.length === 0) return createInvalidAdGroupIdResponse();
		try {
			return normalizeApiResponse(await fetchTossAdPromise({
				adGroupId: spaceUnitId,
				sdkId: "108",
				availableStyleIds: ["1", "2"]
			}));
		} catch (error) {
			return {
				resultType: "FAIL",
				error: { reason: error instanceof Error ? error.message : "Unknown fetchTossAd error" }
			};
		}
	};
}
var pendingLoad2 = null;
function initialize(options) {
	const { callbacks } = options;
	const resolveInitialized = () => callbacks?.onInitialized?.();
	const rejectInitialized = (error) => {
		const normalizedError = error instanceof Error ? error : new Error(String(error));
		callbacks?.onInitializationFailed?.(normalizedError);
	};
	if (getAdsSdk()?.isInitialized()) {
		resolveInitialized();
		return;
	}
	if (pendingLoad2 != null) {
		pendingLoad2.then(resolveInitialized).catch(rejectInitialized);
		return;
	}
	const initPromise = loadAdsSdk().then((sdk) => {
		if (sdk.isInitialized()) return;
		const config = {
			environment: "live",
			customAdFetcher: createCustomAdFetcher(),
			opener: openUrlOpener
		};
		sdk.init(config);
	});
	pendingLoad2 = initPromise;
	initPromise.then(resolveInitialized).catch(rejectInitialized).finally(() => {
		if (pendingLoad2 === initPromise) pendingLoad2 = null;
	});
}
function attach(adGroupId, target, options = {}) {
	const { callbacks } = options;
	const normalizedAdGroupId = normalizeAdGroupId(adGroupId);
	const rejectAttached = (error) => {
		const normalizedError = error instanceof Error ? error : new Error(String(error));
		callbacks?.onAdFailedToRender?.({
			slotId: "",
			adGroupId: normalizedAdGroupId,
			adMetadata: {},
			error: {
				code: 0,
				message: normalizedError.message
			}
		});
	};
	try {
		const spaceId = normalizedAdGroupId;
		if (spaceId.length === 0) throw createInvalidAdGroupIdError();
		const sdk = getAdsSdk();
		if (!sdk) throw new Error("[toss-ad] Call initialize() before attaching an ad.");
		if (!sdk.banner) throw new Error("[toss-ad] Loaded TossAdsSpaceKit does not support banner ads.");
		const element = typeof target === "string" ? document.querySelector(target) : target;
		if (!element) throw new Error(`[toss-ad] Failed to find target element: ${target}`);
		const slotOptions = {
			spaceId,
			autoLoad: true,
			theme: options.theme,
			padding: options.padding,
			callbacks: wrapCallbacks(normalizedAdGroupId, options.callbacks)
		};
		sdk.banner.createSlot(element, slotOptions);
	} catch (error) {
		rejectAttached(error);
	}
}
var DEFAULT_ATTACH_PADDING_BANNER = "16px 20px";
var DEFAULT_ATTACH_PADDING_NATIVE_IMAGE = "20px";
var DEFAULT_ATTACH_THEME = "auto";
var DEFAULT_ATTACH_TONE = "blackAndWhite";
var DEFAULT_ATTACH_VARIANT = "expanded";
var ATTACH_CLASS_NAME = "toss-ads-attach";
var ATTACH_STYLE_ID = "toss-ads-attach-style";
var ATTACH_WRAPPER_ATTRIBUTE = "data-toss-ads-attach-banner-wrapper";
function ensureAttachStyles(container) {
	const documentRef = container.ownerDocument;
	if (!documentRef) return;
	if (documentRef.getElementById(ATTACH_STYLE_ID)) return;
	const style = documentRef.createElement("style");
	style.id = ATTACH_STYLE_ID;
	style.textContent = `
    .${ATTACH_CLASS_NAME} { background: #ffffff; }
    .${ATTACH_CLASS_NAME}.toss-ads-tone-grey { background: #f2f4f7; }
    @media (prefers-color-scheme: dark) {
      .${ATTACH_CLASS_NAME} { background: #17171c; }
      .${ATTACH_CLASS_NAME}.toss-ads-tone-grey { background: #101013; }
    }
    .${ATTACH_CLASS_NAME}.toss-ads-theme-light { background: #ffffff; }
    .${ATTACH_CLASS_NAME}.toss-ads-theme-light.toss-ads-tone-grey { background: #f2f4f7; }
    .${ATTACH_CLASS_NAME}.toss-ads-theme-dark { background: #17171c; }
    .${ATTACH_CLASS_NAME}.toss-ads-theme-dark.toss-ads-tone-grey { background: #101013; }
  `;
	const styleContainer = documentRef.head ?? documentRef.body ?? documentRef.documentElement;
	if (!styleContainer) return;
	styleContainer.appendChild(style);
}
function removeStaleAttachedBannerWrappers(container) {
	Array.from(container.children).forEach((child) => {
		if (child instanceof HTMLElement && child.getAttribute(ATTACH_WRAPPER_ATTRIBUTE) === "true") container.removeChild(child);
	});
}
function attachBanner(adGroupId, target, options = {}) {
	const { callbacks, theme = DEFAULT_ATTACH_THEME, tone = DEFAULT_ATTACH_TONE, variant = DEFAULT_ATTACH_VARIANT } = options;
	const normalizedAdGroupId = normalizeAdGroupId(adGroupId);
	const rejectAttached = (error) => {
		const normalizedError = error instanceof Error ? error : new Error(String(error));
		callbacks?.onAdFailedToRender?.({
			slotId: "",
			adGroupId: normalizedAdGroupId,
			adMetadata: {},
			error: {
				code: 0,
				message: normalizedError.message
			}
		});
	};
	const wrappedCallbacks = wrapCallbacks(normalizedAdGroupId, callbacks);
	try {
		const spaceId = normalizedAdGroupId;
		if (spaceId.length === 0) throw createInvalidAdGroupIdError();
		const sdk = getAdsSdk();
		if (!sdk) throw new Error("[toss-ad] Call initialize() before attaching an ad.");
		if (!sdk.banner) throw new Error("[toss-ad] Loaded TossAdsSpaceKit does not support banner ads.");
		const container = typeof target === "string" ? document.querySelector(target) : target;
		if (!container) throw new Error(`[toss-ad] Failed to find target element: ${target}`);
		return getOrCreateAttachedBanner(container, () => {
			const wrapper = document.createElement("div");
			wrapper.style.width = "100%";
			wrapper.style.height = "100%";
			wrapper.style.boxSizing = "border-box";
			wrapper.style.display = "flex";
			wrapper.style.flexDirection = "column";
			wrapper.style.justifyContent = "center";
			wrapper.style.overflow = "hidden";
			wrapper.setAttribute(ATTACH_WRAPPER_ATTRIBUTE, "true");
			if (variant === "card") wrapper.style.padding = "0 10px";
			const element = document.createElement("div");
			element.classList.add(ATTACH_CLASS_NAME);
			if (tone === "grey") element.classList.add("toss-ads-tone-grey");
			if (theme === "light") element.classList.add("toss-ads-theme-light");
			else if (theme === "dark") element.classList.add("toss-ads-theme-dark");
			if (variant === "card") {
				element.style.borderRadius = "16px";
				element.style.overflow = "hidden";
			}
			wrapper.appendChild(element);
			let isAttached = false;
			let slot = null;
			try {
				ensureAttachStyles(container);
				removeStaleAttachedBannerWrappers(container);
				container.appendChild(wrapper);
				isAttached = true;
				const slotOptions = {
					spaceId,
					autoLoad: true,
					theme: theme === "auto" ? void 0 : theme,
					renderPadding: (styleId) => {
						if (styleId === "1") return DEFAULT_ATTACH_PADDING_BANNER;
						return DEFAULT_ATTACH_PADDING_NATIVE_IMAGE;
					},
					callbacks: wrappedCallbacks
				};
				slot = sdk.banner.createSlot(element, slotOptions);
			} catch (error) {
				if (isAttached && wrapper.parentNode) wrapper.parentNode.removeChild(wrapper);
				throw error;
			}
			return { destroy() {
				slot?.destroy();
				if (isAttached && wrapper.parentNode) wrapper.parentNode.removeChild(wrapper);
				isAttached = false;
			} };
		});
	} catch (error) {
		rejectAttached(error);
		return { destroy() {} };
	}
}
function destroy(slotId) {
	const sdk = getAdsSdk();
	if (!sdk?.banner) return;
	sdk.banner.destroy(slotId);
}
function destroyAll() {
	const sdk = getAdsSdk();
	if (!sdk?.banner) return;
	sdk.banner.destroyAll();
	resetAttachedBannerRegistry();
}
function wrapCallbacks(adGroupId, callbacks) {
	if (!callbacks) return;
	const mapEvent = (payload) => {
		const next = { ...payload ?? {} };
		next.adGroupId = next.adGroupId ?? next.spaceId ?? adGroupId;
		delete next.spaceId;
		return next;
	};
	return {
		onAdRendered: (payload) => callbacks.onAdRendered?.(mapEvent(payload)),
		onAdViewable: (payload) => callbacks.onAdViewable?.(mapEvent(payload)),
		onAdClicked: (payload) => callbacks.onAdClicked?.(mapEvent(payload)),
		onAdImpression: (payload) => {
			tossAdEventLog({
				log_name: "display_ads_all::impression__1px_banner",
				log_type: "event",
				params: {
					event_type: "impression",
					schema_id: 1812034,
					request_id: payload?.adMetadata?.requestId ?? ""
				}
			});
			callbacks.onAdImpression?.(mapEvent(payload));
		},
		onAdFailedToRender: (payload) => callbacks.onAdFailedToRender?.({
			...mapEvent(payload),
			error: payload?.error ?? {
				code: 0,
				message: "UNKNOWN"
			}
		}),
		onNoFill: (payload) => callbacks.onNoFill?.(mapEvent(payload))
	};
}
var TossAds = {
	initialize: Object.assign(initialize, { isSupported: fetchTossAd.isSupported }),
	attach: Object.assign(attach, { isSupported: fetchTossAd.isSupported }),
	attachBanner: Object.assign(attachBanner, { isSupported: fetchTossAd.isSupported }),
	destroy: Object.assign(destroy, { isSupported: fetchTossAd.isSupported }),
	destroyAll: Object.assign(destroyAll, { isSupported: fetchTossAd.isSupported })
};
var getServerTime = Object.assign(createAsyncBridge("getServerTime"), { isSupported: createConstantBridge("getServerTime_isSupported") });
var requestReview = Object.assign(createAsyncBridge("requestReview"), { isSupported: createConstantBridge("requestReview_isSupported") });
//#endregion
//#region node_modules/@apps-in-toss/web-analytics/dist/index.js
var extractDateFromUUIDv7 = (uuid) => {
	const timestampHex = uuid.split("-").join("").slice(0, 12);
	return new Date(Number.parseInt(timestampHex, 16));
};
var getReferrer = () => {
	try {
		return new URL(getSchemeUri()).searchParams.get("referrer");
	} catch {
		return "";
	}
};
var getGroupId$1 = () => {
	try {
		const url = new URL(location.href);
		if (url.protocol !== "https:") throw new Error("Invalid URL");
		return {
			groupId: url.pathname,
			search: url.search.startsWith("?") ? url.search.substring(1) : url.search
		};
	} catch {
		return {
			groupId: "unknown",
			search: "unknown"
		};
	}
};
var Analytics = {
	screen: (params = {}) => {
		const { groupId, search } = getGroupId$1();
		if (groupId === "unknown") return;
		const { log_name, ...otherParams } = params;
		return eventLog({
			...params,
			log_type: "screen",
			log_name: log_name ?? `${groupId}::screen`,
			params: {
				search,
				referrer: getReferrer(),
				document_title: document.title,
				deployment_id: env.getDeploymentId(),
				deployment_timestamp: extractDateFromUUIDv7(env.getDeploymentId()).getTime(),
				...otherParams
			}
		});
	},
	impression: (params = {}) => {
		const { groupId, search } = getGroupId$1();
		if (groupId === "unknown") return;
		const { log_name, ...otherParams } = params;
		return eventLog({
			log_type: "event",
			log_name: log_name ?? `${groupId}::impression`,
			params: {
				...otherParams,
				search,
				referrer: getReferrer(),
				deployment_id: env.getDeploymentId(),
				deployment_timestamp: extractDateFromUUIDv7(env.getDeploymentId()).getTime(),
				event_type: "impression"
			}
		});
	},
	click: (params = {}) => {
		const { groupId, search } = getGroupId$1();
		if (groupId === "unknown") return;
		const { log_name, ...otherParams } = params;
		return eventLog({
			log_type: "event",
			log_name: log_name ?? `${groupId}::click`,
			params: {
				...otherParams,
				search,
				referrer: getReferrer(),
				deployment_id: env.getDeploymentId(),
				deployment_timestamp: extractDateFromUUIDv7(env.getDeploymentId()).getTime(),
				event_type: "click"
			}
		});
	}
};
//#endregion
export { Accuracy, Analytics, FetchAlbumPhotosPermissionError, FetchContactsPermissionError, GetClipboardTextPermissionError, GetCurrentLocationPermissionError, GoogleAdMob, IAP, OpenCameraPermissionError, SafeAreaInsets, SetClipboardTextPermissionError, StartUpdateLocationPermissionError, Storage, TossAds, appLogin, appsInTossEvent, appsInTossSignTossCert, checkoutPayment, closeView, contactsViral, createAsyncBridge, createConstantBridge, createEventBridge, env, eventLog, fetchAlbumPhotos, fetchContacts, generateHapticFeedback, getAnonymousKey, getAppsInTossGlobals, getClipboardText, getCurrentLocation, getDeviceId, getGameCenterGameProfile, getGroupId, getIsTossLoginIntegratedService, getLocale, getNetworkStatus, getOperationalEnvironment, getPermission, getPlatformOS, getSafeAreaInsets, getSchemeUri, getServerTime, getTossAppVersion, getTossShareLink, getUserKeyForGame, graniteEvent, grantPromotionReward, grantPromotionRewardForGame, isMinVersionSupported, loadFullScreenAd, onVisibilityChangedByTransparentServiceWeb, openCamera, openGameCenterLeaderboard, openPermissionDialog, openURL, partner, requestPermission, requestReview, saveBase64Data, setClipboardText, setDeviceOrientation, setIosSwipeGestureEnabled, setScreenAwakeMode, setSecureScreen, share, showFullScreenAd, startUpdateLocation, submitGameCenterLeaderBoardScore, tdsEvent };

//# sourceMappingURL=@apps-in-toss_web-framework.js.map